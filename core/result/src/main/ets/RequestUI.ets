/**
 * @file 请求 UI 辅助（占位实现，待实现）
 * @author Joker.X
 */

export class RequestUI {
  /**
   * 显示全局加载
   * @returns {void} 无返回值
   */
  static showLoading(): void {
  }

  /**
   * 隐藏全局加载
   * @returns {void} 无返回值
   */
  static hideLoading(): void {
  }

  /**
   * 显示错误提示
   * @param {string} message - 错误信息
   * @returns {void} 无返回值
   */
  static toastError(message: string): void {
  }
}
